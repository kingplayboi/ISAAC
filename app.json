{
  "name": "ISAAC-MD",
  "description": "ISAAC WhatsApp Multi-Device Bot",
  "logo": "",
  "repository": "https://github.com/kingplayboi/ISAAC",
  "keywords": ["whatsapp", "bot", "isaac", "baileys"],
  "success_url": "/",
  "buildpacks": [
    { "url": "heroku/nodejs" }
  ],
  "addons": [
    {
      "plan": "heroku-postgresql:essential-0",
      "as": "DATABASE"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "basic"
    },
    "web": {
      "quantity": 0,
      "size": "basic"
    }
  },
  "env": {
    "HEROKU_APP_NAME": {
      "description": "The name of this Heroku app the one you entered at the top. Required if you want to use .update/.updatenow.",
      "value": "",
      "required": true
    },
    "SESSION_ID": {
      "description": "Your WhatsApp session string, generated at https://kingpin-sjlx.onrender.com (QR CODE or PAIR CODE)",
      "value": "",
      "required": true
    },
    "BOT_PREFIX": {
      "description": "Command prefix for the bot",
      "value": ".",
      "required": true
    },
    "OWNER_NUMBER": {
      "description": "Your WhatsApp number with country code, no + or spaces (e.g. 254754574642)",
      "value": "254754574642",
      "required": true
    },
    "HEROKU_API_KEY": {
      "description": "Your Heroku account API key (dashboard.heroku.com/account → API Key). Required if you want to use .update/.updatenow to check and trigger redeploys from WhatsApp.",
      "value": "",
      "required": true
    }
  }
}
